[loggers]
keys=root,my_project,urllib3,requests,matplotlib,asyncio

[handlers]
keys=consoleHandler,fileHandler

[formatters]
keys=standardFormatter,detailedFormatter

# --- Loggers ---

[logger_root]
level=WARNING
handlers=consoleHandler

[logger_my_project]
level=DEBUG
handlers=consoleHandler,fileHandler
qualname=my_project
propagate=0

# Third-party silencers
[logger_urllib3]
level=WARNING
handlers=
qualname=urllib3

[logger_requests]
level=WARNING
handlers=
qualname=requests

[logger_matplotlib]
level=WARNING
handlers=
qualname=matplotlib

[logger_asyncio]
level=WARNING
handlers=
qualname=asyncio

# --- Handlers ---

[handler_consoleHandler]
class=StreamHandler
level=DEBUG
formatter=standardFormatter
args=(sys.stdout,)

[handler_fileHandler]
class=handlers.RotatingFileHandler
level=INFO
formatter=detailedFormatter
# args=(filename, mode, maxBytes, backupCount)
args=('yt2navidrome.log', 'a', 10485760, 5)

# --- Formatters ---

[formatter_standardFormatter]
format=%(asctime)s [%(levelname)s] %(name)s: %(message)s
datefmt=%Y-%m-%d %H:%M:%S

[formatter_detailedFormatter]
format=%(asctime)s - %(name)s - %(levelname)s - %(module)s:%(lineno)d - %(message)s
